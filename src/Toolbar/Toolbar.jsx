/*

const CURRENT_NUMBER_OF_ARRAY_BARS = document.getElementById("MyRange");

 CURRENT_NUMBER_OF_ARRAY_BARS = document.getElementById("MyRange");
    NUMBER_OF_ARRAY_BARS = CURRENT_NUMBER_OF_ARRAY_BARS.value;

<button onClick={() => this.resetArray()}>Generate new Array</button>
        <div class="slidecontainer"><input type="range" min="5" max="150" class="slider" id="MyRange" onChange={this.handleChange}></input></div>
        <button onClick={() => this.MergeSort()}>Merge Sort</button>
        <button onClick={() => this.QuickSort()}>Quick Sort</button>
        <button onClick={() => this.HeapSort()}>Heap Sort</button>
        <button onClick={() => this.BubbleSort()}>Bubble Sort</button>*/